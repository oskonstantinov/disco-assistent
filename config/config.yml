# Application configuration
language: ru  # Application language (can be en or ru)

# API configuration
api:
  anthropic_key: ""  # API key

# Custom prompt configuration
custom_prompt:
  enabled: false  # Set to true to use custom prompt instead of default
  content: ""     # Custom prompt content if enabled

# User context configuration
user_context:
  enabled: false  # Set to true to include user context
  description: |
    Sample description of the user context that will be included in the prompts.

model:
  name: "claude-sonnet-4-5"
  temperature: 1
  max_tokens: 8192
